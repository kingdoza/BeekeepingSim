### [질문 8]

1. Engaged 상태에서 크로스헤어 WBP 제어 방식
- 질문 내용: `UAnchoredFocusActionComponent`를 상속한 새 컴포넌트가 Engaged 상태일 때 크로스헤어 WBP를 어떻게 비활성화할지 확정이 필요하다.
- 필요한 이유: 액션 컴포넌트가 UI 위젯을 직접 참조할지, 컨트롤러/HUD 쪽에 상태 요청만 보낼지에 따라 결합도와 재사용성이 달라진다.
- 선택지
  - 옵션 A: 새 컴포넌트가 PlayerController 또는 HUD에 `SetCrosshairVisible(bool)` 같은 함수를 호출해서 크로스헤어 표시를 제어
  - 옵션 B: 새 컴포넌트가 델리게이트만 브로드캐스트하고, Blueprint UI/HUD가 이를 받아 크로스헤어 WBP를 숨김/표시
  - 옵션 C: 새 컴포넌트가 크로스헤어 WBP 인스턴스를 직접 참조해서 Visibility를 변경

[답변]
옵션 B

---

### [질문 9]

1. Engaged 상태의 마우스 자유 모드 입력 모드
- 질문 내용: Engaged 상태에서 "마우스 커서 보임 + 마우스 자유 모드"를 Unreal 입력 모드 기준으로 어떻게 처리할지 확정이 필요하다.
- 필요한 이유: `GameOnly`, `GameAndUI`, `UIOnly` 중 어떤 입력 모드를 쓸지에 따라 게임 입력, UI 클릭, 취소 입력 동작이 달라진다.
- 선택지
  - 옵션 A: `FInputModeGameAndUI` 사용, 마우스 커서 표시, 게임/취소 입력과 UI 입력 동시 허용
  - 옵션 B: `FInputModeUIOnly` 사용, 커서 표시, UI 조작 위주로 전환하고 취소 입력만 별도 허용
  - 옵션 C: `FInputModeGameOnly` 유지 + 커서만 표시하는 커스텀 방식

[답변]
옵션 A

---

### [질문 10]

1. Engaged 해제 시 크로스헤어/커서 복구 시점
- 질문 내용: FocusCancel 후 카메라 복귀 블렌드가 끝나기 전과 후 중 언제 크로스헤어 재표시와 마우스 커서 숨김/입력 모드 복구를 할지 확정이 필요하다.
- 필요한 이유: 현재 답변상 입력 재활성화는 카메라 복귀 완료 후이므로, UI 상태 복구도 그 시점에 맞출지 분리할지 결정해야 한다.
- 선택지
  - 옵션 A: 카메라 복귀 블렌드 완료 후 크로스헤어 재표시, 커서 숨김, 입력 모드 복구를 함께 수행
  - 옵션 B: FocusCancel 즉시 크로스헤어 재표시와 커서 숨김, 입력 모드 복구는 블렌드 완료 후 수행
  - 옵션 C: 크로스헤어는 즉시 복구, 커서/입력 모드는 블렌드 완료 후 복구

[답변]
옵션 B